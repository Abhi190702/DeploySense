# DeploySense v0.1.1 Release Notes

DeploySense v0.1.1 fixes the npm package metadata from the initial beta publish.

The `deploysense` CLI and scanner packages now publish direct npm-compatible internal dependency versions instead of workspace protocol references.
